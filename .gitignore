node_modules/
*.log
.DS_Store
.cache/
coverage/
# build output is committed for manual installs / verified by CI, but ignore local rebuild noise
*.tsbuildinfo
# dev-only harness (depends on the built bundle)
harness.html
art/
# the built bundle (starlink-card.js) IS committed so HACS can validate the
# branch and manual installs work; release CI rebuilds + attaches it per release.

